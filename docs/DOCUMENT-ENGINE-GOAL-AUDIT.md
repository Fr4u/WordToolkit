# Document-engine goal audit

Last updated: 2026-07-24. This is the completion ledger for the WordToolkit
document-engine redesign. `Implemented` means code and tests exist in the current
branch. `Partial` means a real slice exists but the stated capability is not complete.
Historical Python behavior does not count as implemented in the new native engine.

The goal must not be marked complete while any required row is `Not started`, `Planned`,
or `Partial`, unless the row is explicitly removed by the user. Documentation or a
vendor claim is not implementation evidence.

## Standards and research

| Requirement | State | Current evidence | Exit condition |
|---|---|---|---|
| ECMA-376 Parts 1–4 and OPC model | Partial (strengthened) | `RESEARCH-MATRIX-2026.md`; bounded OPC graph plus a package-wide, source-preserving ECMA-376 Part 3 fifth-edition MCE graph for Ignorable/ProcessContent/MustUnderstand/AlternateContent, explicit application and markup configurations, legacy preservation-hint inventory, resource limits and real/synthetic tests | Broader Strict/transitional and hostile corpus, MCE serializer/transform and namespace-changing mutation rules, full URI conformance, signatures/encryption and version-pinned extension behavior measured |
| Microsoft Word extensions | Partial | MS-DOCX/MS-ODRAWXML/MS-OE376 sources recorded | Typed or opaque-preserving coverage with version probes and regression fixtures |
| Word COM, JS add-ins, Graph, Office Scripts, Copilot | Partial | Primary-source comparison recorded | Capability/version probes and measured adapter behavior |
| OfficeCLI, docx-cli and Word MCP servers | Partial (strengthened) | Twelve pinned AI/Word repositories inspected; WordToolkit and safe-docx now have one shared neutral 42-scenario protocol run | Expand shared-corpus round trips, token, latency and safety benchmarks across the remaining adapters |
| Open XML SDK, python-docx, docx4j, POI, docx.js, Pandoc, Mammoth | Partial | Primary docs and architectural limits recorded | Reproducible representative tests and license/maintenance snapshot |
| LibreOffice UNO and ONLYOFFICE | Partial | Primary API docs recorded | Version-pinned conversion/render corpus measurements |
| Commercial document engines | Partial (strengthened research) | Official documentation recorded for Aspose, GemBox, Spire, Syncfusion, DevExpress, Telerik, TX Text Control and GroupDocs; claims remain vendor evidence | Licensed evaluation on shared public fixtures |
| Remaining relevant libraries/services | Partial (strengthened research) | Pinned implementation review covers PHPWord, docxtpl, docxcompose, docx-rs, docx-templates, docxtemplater, Xceed DocX, Open-Xml-PowerTools, addFormula2docx, unoconv, unoserver, pdf2docx and docx2pdf; Google Docs and Adobe conversion surfaces are recorded from primary docs | Cloud API runs, remaining material engines and shared-corpus measurements |

## Package, XML, and semantic foundation

| Requirement | State | Current evidence | Exit condition |
|---|---|---|---|
| Bounded ZIP/OPC reader | Implemented (initial) | `WordToolkit.Engine/Packaging`; entry/size/ratio/XML limits; cancellation; tests | External fuzz campaign, hostile corpus, memory/latency benchmarks |
| Complete entry/part/content-type graph | Implemented (initial) | Raw bytes, hashes, canonical URIs, Default/Override maps | Full OPC URI conformance and Flat OPC parity |
| Relationship graph and reachability | Implemented (strengthened initial slice) | Root/part relationships, RFC 3986 type/target checks, XML-ID checks, fragment retention, reserved relationship-part/content-type rules, target modes, resolution, missing/orphan diagnostics | Digital-signature/encryption semantics, format-specific cardinality/type rules, repair and broad corpus proof |
| Unknown/opaque part preservation | Implemented (initial) | Raw entry snapshots and deterministic random round-trip smoke | Large mixed-extension corpus with untouched-byte reports |
| Immutable snapshots and fingerprints | Implemented (initial) | Order-independent package fingerprint; read-only model | Content-addressed cache, snapshot lifecycle and cross-platform reproducibility proof |
| Lossless XML token/source model | Implemented (initial) | Raw bytes/hash; lexical element/attribute tree; prefixes, namespace URIs, quotes and exact byte spans; UTF-8/16/32 and single-byte mapping; DTD/size/depth/count bounds; validated non-overlapping patches; BOM/entity/whitespace/comment/CDATA/randomized splice tests; every typed XML part in 52 bundled multi-producer DOCX files parses and no-ops byte-exactly; a versioned nine-document/five-producer semantic golden corpus fixes exact typed facts beyond crash/no-op smoke | Auxiliary/versioned story adapters, stateful encoding policy, token-level edits around mixed content, external hostile/version/visual corpus and memory/latency proof |
| Typed WordprocessingML parser | Partial (strengthened) | Read-only main part plus relationship-driven headers, footers, footnotes, endnotes, comments, glossary and nested text boxes; typed section properties and strict/transitional relationship tests | Remaining auxiliary/versioned parts and the full required Word vocabulary |
| Source-linked semantic AST | Partial (strengthened) | Stable node IDs and exact lexical ordinals now span the primary text-bearing stories; story roots and note/comment/reference nodes retain part and relationship provenance | Full document graph, durable locator recovery, ambiguity model, threaded comments and auxiliary-story mutation provenance |
| Stable semantic identity | Partial | `w14:paraId`/`textId`, durable IDs, fallback fingerprints, duplicate occurrence tests | Cross-save, cross-producer, move/edit and ambiguity benchmark |
| Serializer | Partial (strengthened) | Package serializer preserves entry payloads and deterministic mode; lossless leaf-text and structural element remove/unwrap/local-name-rename/replacement patches preserve every unrelated byte and reparse the candidate | General namespace/MCE mutation rules and all typed part serializers |

## Transactions, safety, and recovery

| Requirement | State | Current evidence | Exit condition |
|---|---|---|---|
| Entry-hash preconditions | Implemented (strengthened initial slice) | Mutation builder plus XML source hash, package fingerprint, semantic node, source ordinal and expected-text gates | General semantic command predicates and destination/cloud version preconditions |
| Atomic file persistence | Implemented (strengthened initial) | Sibling temp, flush, validate, recheck, replace, optional backup; merge has a race-safe require-new mode that refuses every overwrite | Power-loss and filesystem fault injection across supported platforms |
| Rollback | Partial (strengthened) | Candidate rejection leaves original unchanged; backup path; text and review transactions retain exact original-part inverses; portable `.wtpatch` artifacts store every changed before/after OPC entry payload and expose an exact reverse patch guarded by the result fingerprint. Public `plan_ooxml_patch_rollback` and `apply_ooxml_patch_rollback` derive that reverse from the original artifact, require the current result fingerprint plus original patch ID, bind a distinct plan ID to the destination path, re-run semantic/risk/type/schema proof, enforce independent authorizations and publish atomically with a redo-capable backup by default. That complete saved-package path is now owned once by `PatchRollbackWordPackageOperation` and shared unchanged by direct .NET, strict JSON CLI and MCP; the duplicate Native reverse branch is removed. Every current native custom-Undo mutation family snapshots whole-document Flat OPC, main-story and linked-story OOXML, exact target/context, boundaries, save state and structural counts; SmartArt and review-property paths add supplemental state fingerprints. Failed record closure, false/throwing Undo or any mismatch returns `ROLLBACK_FAILED` and quarantines the live handle/document identity. The mixed operation path stages complete text, paragraph, style, formatting and native-equation batches in a Flat OPC clone, publishes them through one `FormattedText` assignment and verifies exact target ranges/formatting/equation semantics before version advancement. If Undo cannot prove restoration, that path now opens the retained baseline as a separate hidden recovery document and copies its main story back before rechecking exact boundaries, text, counts and a stable semantic whole-document Flat OPC hash that ignores only WordprocessingML `w:rsid*` metadata. Save state is restored only after the remaining checkpoint passes. Fake COM proves accepted independent recovery after `Undo=false`; real Word 16.0 restores text, paragraph count and OMath count but still changes semantic Flat OPC beyond `w:rsid*`, so the runtime correctly quarantines instead of declaring package equality. The legacy remote gateway runs all 33 ordinary draft mutators on an isolated clone and swaps the active engine/version only after candidate serialization and validation | Obtain package-exact live recovery or a sanctioned live document-identity swap when Word exposes no usable or byte-exact Undo record; add crash-durable remote drafts and injected failure proof for disk-full, denied, process/Word crash and race phases |
| Multi-command document transaction | Partial (text/review/patch/merge slices, native plan/apply) | Bounded text edits and selective review decisions resolve against one snapshot and retain inverse payloads. The generic OPC patch slice creates deterministic portable add/replace/delete artifacts between two snapshots. The initial three-way merge composes proven disjoint lossless text commands or requires explicit conflict choices, then turns the candidate back into the same reversible patch/risk transaction | Broader heterogeneous semantic commands, structural/revision-aware merge, permissions/approval policies and a unified validation profile across every command family |
| Optimistic concurrency | Partial (strengthened) | Forward plans require exact package fingerprints and part hashes; portable patch apply binds base/result fingerprints, patch ID and deterministic apply-plan ID. Merge binds ancestor/left/right fingerprints, every resolution and a normalized new output path. The atomic writer rechecks existing destinations, verifies the bytes displaced by replacement and compensates a non-cooperative final writer. A second change during compensation is retained as an opaque `.conflict` artifact rather than deleted; live document versions exist | File identity/version integration, broader filesystem race/fault injection and Graph/Drive ETag support |
| Security policy | Partial (strengthened) | ZIP/XML bounds, DTD ban, external links never fetched, MCP redaction; patch archives reject unsafe/duplicate paths, unknown/duplicate manifest fields, hash/length drift, expansion bombs and unreferenced payloads. Patch and merge apply have separate fail-closed authorizations for signature invalidation, macro/OLE/ActiveX, external relationships, opaque binaries and new errors; unresolved merge conflicts, validation truncation/SDK-open failure and type mismatch are non-overridable | Protection enforcement, explicit signature verification/removal/resign workflow, encryption adapter, sandboxed backends and threat-model audit |
| Privacy/content-minimizing telemetry | Partial foundation (strengthened query boundary) | Text/review/patch/merge summaries omit document text and payloads; merge conflict previews and hashes are separate opt-ins; review selection accepts redacted author fingerprints; semantic query properties are opt-in and redact author/name/date/GUID/field-instruction/anchor values behind a second explicit flag; every result marks document content untrusted; MCP plan/apply is stateless and retains no server-side document-content cache. Recovery-required details contain at most two existing opaque sibling artifact names and no absolute path or payload | Opt-in telemetry implementation, broader redaction tests, expiry and debug-bundle audit |

## Document intelligence and editing

| Requirement | State | Current evidence | Exit condition |
|---|---|---|---|
| Compact inspect | Implemented (strengthened initial slice) | `inspect_ooxml_package`, `inspect_ooxml_semantics`, lazy `query_ooxml_semantics`, `manage_ooxml_semantic_index`, `inspect_ooxml_sections`, `inspect_ooxml_styles`, `inspect_ooxml_numbering`, `inspect_ooxml_theme`, `inspect_ooxml_settings`, `inspect_ooxml_fonts`, `inspect_ooxml_references`, `inspect_ooxml_bibliography`, `inspect_ooxml_dependencies`, `inspect_ooxml_charts`, `inspect_ooxml_diagrams`, `inspect_ooxml_figures`, `inspect_ooxml_content_controls`, `inspect_ooxml_active_content`, `inspect_ooxml_properties`, `inspect_ooxml_tables`, `inspect_ooxml_markup_compatibility` and filtered `resolve_ooxml_formatting`; projected-part inventory, privacy redaction, fingerprints, exact filters, per-field/item bounds and offset paging; field-heavy TOC fixtures enforce sub-5000-character default reference/dependency responses, the bibliography fixture enforces a sub-5000-character redacted default response, chart/figure/content-control/active-content/document-property/table/MCE fixtures enforce sub-5000-character defaults plus bounded complete JSON-RPC envelopes, and SmartArt enforces a sub-5000-character default plus a sub-32-KiB maximal keyed/source projection | Opaque continuation tokens, remaining auxiliary/versioned parts and a representative cross-action token benchmark suite |
| Semantic query/search | Partial (public indexed structural-query slice) | Source-ordered kind/property/part/subtree selectors across main, header/footer, note, comment, glossary and text-box stories; strict ancestor/descendant predicates combine related-node kinds with exact properties and are propagated in linear time. Streaming contains/equals/starts/ends matching crosses run/field/tab/break boundaries. `QueryWordPackageOperation` is one versioned Engine/CLI/MCP contract for direct and indexed execution, returns high-level object/story/identity fields, supports a local fingerprint precondition and keeps optional previews/properties/provenance bounded with sensitive-property redaction | Fields/math/metadata-aware predicates beyond existing node kinds/properties, parent/child and sibling axes, aggregations, cost model and text postings |
| Document dependency graph | Partial (declared figure/shape topology + document-property/variable domain + operation-wide accounted boundary) | Deterministic `wddn_`/`wdde_` topology joins OPC, semantic, style, numbering, reference, section, chart, figure/caption, content-control/Custom-XML, table, bibliography, typed active-content and document-property/variable domains while preserving unresolved/external evidence. Figure representations retain typed DrawingML/VML placement and own `FigureShape` nodes with representation-to-root and parent-to-child edges for bounded group/shape topology. The separate live drawing-layout inspector now proves bounded Word-executed object-model geometry but deliberately does not inject traversal-scoped COM identities into this durable saved-package graph. Unique CITATION tags, valid unique `DOCPROPERTY` names and persistent `DOCVARIABLE` names terminate at concrete source nodes. Complete `TA` authority entries now terminate at complete matching `TOA` field nodes; category-zero tables match all valid authority categories, while deleted, incomplete, malformed, missing or ambiguously categorized evidence remains unresolved. Compact compressed-row adjacency and the 128 MiB `wdg1` graph budget remain. `inspect_ooxml_dependencies` creates one 640 MiB `wop1` lease before OPC retention and passes it through lossless XML, semantic fingerprints/nodes and every typed projection including bibliography, active content, settings and document properties. Package/XML charges precede guarded byte copies; semantic hashing is cancellable; domain-local and operation-wide limits reject the next allocation. Compact gateway output drops only zero-valued source diagnostic counters and remains under the existing envelope gate | Shared immutable parsed-story storage, dense mixed-domain calibration, durable package-to-live shape identity joins, off-screen page-layout execution, active-content binary internals/execution, signature cryptography, encryption, co-authoring sessions, semantic query joins, incremental invalidation and mutation-impact policies |
| Indexing | Partial (bounded process-memory index) | `WordSemanticIndex` is package-fingerprint-bound and deterministically indexes up to 100,000 semantic nodes plus one million property occurrences. Lazy management creates/reuses, inspects, lists and explicitly releases random handles; the native cache is capped at four handles/250,000 nodes and a 30-minute TTL, returns no raw text and never persists. Indexed queries require the exact package fingerprint and report their candidate seed/scanned count | Durable encrypted external index, content-aware token/text postings, incremental invalidation, multi-document ranking and crash-safe lifecycle |
| AI planner | Partial foundation (strengthened) | Deterministic bounded text plans and heterogeneous create/clone/exact-consolidate/proven-unused-delete/primary-name-rename/assign style plans, typed review-decision plans, exact package-patch plans and three-way merge plans report counts/impact without returning content. Style plans can create or clone a definition, strictly consolidate an explicit equivalent custom source, delete an explicitly selected custom non-default definition only after complete consumer proof, rename only the primary UI name while retaining the stable style ID, and server-select assignment targets in one reviewed atomic intent. Merge exposes stable conflict IDs, explicit three-choice resolutions, independently authorized risk classes, exact identities and baseline/candidate validation; lazy stateless apply requires reviewed IDs and all source fingerprints | Natural-language intent -> evidence -> broader heterogeneous typed plan -> cost/risk -> richer permissions and approval policy |
| Typed semantic mutations | Partial (text + style-definition/assignment + review transaction slices) | Text plans edit source-bound `w:t`, `w:delText` and `m:t`; `create_style` emits a minimal typed inherited definition, `clone_style` preserves an existing definition's modeled and opaque formatting under safe custom identity, `consolidate_style` rewrites modeled references and removes an exactly equivalent custom source, `delete_unused_style` removes only explicitly selected custom non-default definitions with no surviving modeled, glossary, latent, field or unmodeled consumer, `rename_style` changes only the primary visible name of a custom non-default style while retaining its ID/aliases/formatting/references, and exact `set_style`/token-lean `set_style_where` assign existing or same-plan-created compatible styles. One plan composes sequential `styles.xml`, numbering and selected story changes, validates intermediate graphs and the final candidate, predicts one fingerprint and retains an exact inverse. Review plans accept/reject supported insertion/deletion/conflict wrappers, complete moves and property snapshots, numbering changes, inserted rows and selected cell changes. Deleted-paragraph-mark acceptance and inserted-paragraph-mark rejection merge only an immediate following paragraph with no paragraph properties or revision content; every ambiguous shape fails closed | Arbitrary typed style formatting, stable-ID changes/referenced-or-built-in deletion/fuzzy repair/template alignment and `stylesWithEffects` mirroring; broader paragraph/run/table/field/math commands, remaining review vocabulary, permissions and durable recovery artifacts |
| Validator | Partial (strengthened) | OPC diagnostics plus exact serialized review, patch and merge candidate reparse with bounded Microsoft Open XML SDK baseline/candidate comparison; new errors block by default, while validation truncation or SDK-open failure hard-blocks apply | Unified OPC/schema/extension/semantic/Word-open profiles and incremental validation |
| Linter | Partial (initial native rule packs) | `WordDocumentLinter` evaluates 18 deterministic core/style/accessibility/security rules over one fingerprint-bound typed graph set. Findings carry stable rule/finding IDs, severity, confidence, privacy-safe subject fingerprints, bounded evidence, optional exact XML byte spans, validated rule/finding suppressions and explicit fix metadata. Lazy `lint_ooxml_document` is summary-first, paged, source-redacted by default, never opens Word or follows external targets, and distinguishes execution completeness from incomplete document-domain coverage. One safe empty-title shape reports an implemented reviewed fix | Broader style/numbering/reference/language/link/layout/security rules, corpus calibration, incremental execution and plugin rule registration |
| Formatter | Partial (group-aware fail-closed policy strengthened) | `WordFormatterPlanner` plus lazy `plan_ooxml_format`/`apply_ooxml_format` implement explicit `remove_redundant_direct_formatting`. Fully modeled scalar paragraph/run properties require exact cascade-contribution equality. `rFonts`, `color`, `u` and paragraph/run `shd` are admitted only after a deterministic cumulative candidate rebuild reparses the entire package, reprojects semantic content and proves complete effective group equivalence for every affected node; paragraph candidates also cover descendant runs. At most 64 composite proofs are attempted and crossing the ceiling aborts the whole plan. Conditional-table, revision, numbering-compatibility and unmodeled cascade layers are skipped. Candidate OPC structure, semantic content, affected-node formatting, exact changed parts/fingerprint and baseline-aware Open XML SDK errors are proved before an output-bound apply-plan can create a new file. Signed or truncated/unvalidated candidates are blocked, the source is never overwritten, planning never opens Word, and stable no-op apply creates no file. The installed final runtime removed 11 elements after five group proofs and Microsoft Word produced a byte-identical 144-DPI page raster | Conditional table/revision/application-default resolution, broader previewed policies, representative multi-version Word-rendered equivalence corpus and incremental invalidation/planning |
| Optimizer | Partial (strict style slices) | Explicit `consolidate_style` removes proven canonical-equivalent custom definitions after type-checking and rewriting recognized consumers. Explicit `delete_unused_style` removes proven-unused custom non-default definitions, including a closed mutually dependent batch, only after all modeled and unmodeled consumer gates pass. Exact inverse and candidate validation preserve rollback and structural proof | Lint-driven candidate selection, dead-part/image/font optimization, ZIP recompression policy, broader style minimization and measured size/fidelity corpus |
| Repair engine | Partial (strengthened fail-closed slices) | `WordLintRepairPlanner` accepts only a package-bound empty-title finding with exactly one existing, empty, leaf `dc:title`. Separately, the semantic transaction planner can consolidate an explicitly chosen exact duplicate custom style, delete an explicitly chosen proven-unused custom style, or change only one custom style's primary name while retaining its stable ID and blocking uncertain name consumers. All paths verify fingerprints, perform lossless edits, predict result fingerprints and retain exact inverses; lazy plan/apply adds digital-signature blocking and baseline-aware Open XML validation without returning XML. Unused-style lint findings remain unbound and do not authorize deletion | Missing-title part creation, linter-to-style-repair binding, fuzzy or ranked style repair, broader dependency-aware repair sets, richer authorization/recovery, Word round-trip and corpus proof |
| Semantic diff | Partial (strengthened initial slice) | Native two-layer saved-package comparison reports OPC entry changes separately from source-linked semantic differences across all projected Word stories. Matching ambiguity, fallbacks and unclassified changes remain explicit. The exact snapshots feed both a reversible OPC patch and the result evidence for guarded three-way merge | Richer typed property vocabulary, review-friendly rendering, semantic-command patch format, Word `CompareDocuments` adapter, benchmarked accuracy and cross-version corpus |
| Three-way merge | Partial (initial lossless slice) | Explicit ancestor/left/right planner deterministically selects one-sided and identical entry states; disjoint source-linked text-leaf edits in one part auto-compose only after each branch reconstructs byte-exactly. Stable `wtmc_` conflicts cover divergent add/modify/delete and same-node edits; every conflict requires an explicit ancestor/left/right choice. Result becomes a reversible patch and passes risk, type, OPC and baseline-aware SDK gates before create-new atomic persistence | Typed structural and revision-aware conflicts, dependency edges, durable target recovery across Word rewrites, per-node resolution policies, visual/Word round-trip corpus and merge artifact format |

## Word feature systems

| Requirement | State | Current evidence | Exit condition |
|---|---|---|---|
| Paragraphs/runs/tables | Partial (table read graph strengthened) | Source-linked projection plus first lossless text-leaf edit and a bounded logical table graph covering declared grids, physical/logical cell placement, vertical/legacy-horizontal merges, nested tables, repeating headers, row exceptions, widths/layout and Word-effective floating positioning; live COM editing remains | Structural typed edits, final autofit/page layout, merge/split mutation and licensed multi-version visual tests |
| Sections/headers/footers/notes | Partial (strengthened) | Primary story roots/bodies/references and lossless text edits; section boundaries/page properties plus first/even/default explicit/inherited/blank/effective bindings; unbound-part inventory | Section structural edits, link-to-previous mutation, separator/numbering semantics and layout proof |
| Styles/themes/direct formatting | Partial (typed graphs + strict definition mutation + group-aware redundant-direct-format removal) | Source-linked style, theme, settings and font-table inventories; document defaults, latent metadata, four style types, all 12 theme colors, major/minor and supplemental fonts, `themeFontLang`, embedded-face metadata, format-scheme counts and inheritance diagnostics; paragraph/run resolver applies defaults, base-first paragraph styles, effective numbering levels, character styles and direct formatting, then derives deterministic theme fonts/RGB values and font-table provenance with explicit ambiguity. Typed plans can create minimal inherited styles, clone arbitrary existing definitions losslessly, consolidate explicit canonical-equivalent custom styles with reference rewrites, delete explicitly selected proven-unused custom definitions, rename their primary visible names, assign styles, remove fully modeled scalar direct properties proven redundant against the resolved cascade, and remove `rFonts`, `color`, `u` or paragraph/run `shd` only after bounded whole-group equivalence proof. The linter reports unused/equivalent styles and direct overrides but does not authorize formatter changes | Arbitrary typed formatting mutation, stable-ID change/referenced-or-built-in deletion/fuzzy repair, `stylesWithEffects` mirroring, conditional table/revision/application-default resolution, broader drift rules and template alignment |
| Numbering/lists | Partial (typed graph + effective-level slice) | Exact relationship/root validation; source-linked picture/abstract/instance/level/override inventory; `numStyleLink`/`styleLink` chains, start overrides, corruption diagnostics, compact MCP inspection and effective formatting integration | Counter-state traversal across paragraphs, restart semantics in sequence, label rendering, structural edits, repair/rebuild and layout proof |
| Fields/bookmarks/cross-references | Partial (typed read graph) | Story-scoped paired bookmark ranges; nested complex and simple field parser across paragraphs; explicit/implicit REF tokenizer; source-linked parent/child fields; typed dependency edges and corruption diagnostics; redacted lazy inspection; live allowlisted writes remain separate | Full field grammar/evaluation policy, unified element hyperlinks/notes/captions, update capability, safe structural edits and Word round-trip/layout proof |
| TOC/TOF/TOA/captions | Partial (typed read slice + guarded live caption/TOF/TOC/TOA insertion and refresh) | TOC/TC/SEQ/TA/TOA field classification enters the reference graph; source-linked caption-style/SEQ paragraphs expose labels, bounded results and ambiguity-preserving figure associations. Dedicated live actions create localized captions, tables of figures, semantic heading-based tables of contents, exact token-bound authority entries and one/all-category authority tables without accepting raw field code. Each uses one custom Undo record and requires exact native field/collection readback; TOA creation additionally verifies separators, display flags and tab leader. An installed Word 16.0 proof created three native `TA` marks and one all-category `TOA`, resolved all three saved-package dependencies, passed Microsoft SDK validation and produced a clean three-page A4 PDF with correct pages and dotted leaders | Typed general field-switch AST, TC/custom-style/chapter-numbering creation, safe renumbering policy, tables of tables/equations, authority-entry edit/removal/category management, multi-version/locale corpus and saved-package mutation planning |
| Comments/threaded comments/revisions | Partial (typed read graph + bounded mutation strengthened) | Source-linked comment/thread/person and authorship-linked revision graphs remain redacted; a public body-only plan/apply operation selects stable comment IDs, replaces exact text across adjacent runs in one ordinary direct paragraph, refuses rendered/structural-boundary crossing, returns hashes rather than content, validates the candidate and proves anchors/authors/thread/durable/reaction/revision/permission and unselected-body invariants. Saved-package revision plan/apply selectively accepts or rejects supported wrappers, complete moves and property snapshots | Comment creation/deletion, rich structural bodies, resolution/reaction mutation, remaining paragraph/table/numbering/custom-XML revision transforms, merge and proof across Word versions |
| Content controls/custom XML | Partial (strengthened typed read graph) | Source-linked SDT type/level/lock/placeholder/parent model; physical store discovery through OPC relationships; item properties, normalized store GUIDs and schema references; Word core/extended virtual stores; standard and Office 2013 bindings; restricted absolute child XPath; target ordinals; repeating-section/item topology and cardinality; malformed/duplicate/unresolved diagnostics; privacy-redacted paged MCP and dependency edges | Lossless Custom XML/SDT mutation, binding refresh, richer XPath, rich-text binding decode/write, template fill planner and cross-version Word proof |
| Equations/OfficeMath | Partial (strengthened read graph + live authoring gate) | Source-linked graph covers all 19 standard OMML objects, argument roles, matrix rows/cells, runs/text, display paragraphs, main math defaults, Strict markup, story boundaries, invalid placement and preserved extensions; stable equation/node IDs; compact redacted lazy inspection. The native live adapter securely converts LaTeX/UnicodeMath/MathML/OMML, protects integral and limit-operator boundaries, reconstructs five Word mathematical-alphabet families, scopes differential placement to integral-owned operands and rolls back on canonical readback drift. `\mathbf` and `\boldsymbol` cross Word build-up through internal balanced sentinels. MathML inheritance/token overrides preserve all four weight/slant styles plus ten representable alphabet variants; supported OMML structures preserve every `m:sty` value independently from recognized `m:ctrlPr/w:rPr` bold/italic controls, while unsupported structures fail. Independent semantic/style-contract readback normalizes Word's documented italic/roman defaults and equivalent sibling-run coalescing while retaining text, normal/literal flags, script and control placement. Nested fractions, radicals, delimiters, n-ary objects and mixed weights pass real Word. Word-surviving U+2003/U+2005 spacing keeps `cases` text readable and is part of the readback contract. Forced real-Word gates pass the 48-family atlas, all 112 registered symbol commands, all 20 named functions, ten delimiter forms, all 14 representable MathML variants, styled alphabets and an ordinary derivative | Unified cross-format semantic AST, safe structural mutations, unsupported OMML structures, contextual Arabic MathML variants, general loss-aware round trips, mathematical-equivalence diagnostics and broader versioned Word visual proof |
| DrawingML/VML/images/text boxes | Partial (declared graph + live Word object-model execution) | Source-linked logical figures collapse DrawingML Choice/VML fallback without claiming MCE evaluation. Inline/anchor declarations type position, reference frames, alignment/offsets, extents, relative size and bounded wrapping polygons; shape representations retain stable group/child topology, transforms, recognized preset/custom paths, fill/line/effect and text-flow declarations. Complementary `inspect_live_word_drawing_layout` asks the connected Word build to repaginate and returns anchors, page/section, typed reference frames, offsets versus alignment constants, wrapping, sizes, group-local children and optional viewport pixels with explicit coordinate semantics. The real VML group fixture preserves the source hash and exposes the honest normalization boundary: two declared group nodes become one runtime group plus one AutoShape. Nested `w:txbxContent` remains source-linked and live traversal IDs are not durable package IDs | Safe figure/shape mutation, guide/effect/theme evaluation, final line-level text flow, off-screen page boxes, durable package/live identity joins, MCE-qualified representation selection, reading order and multi-version render corpus |
| Charts/SmartArt/OLE/embedded packages | Partial (classic charts + native SmartArt structure + live node layout + guarded text mutation + active-content metadata) | Bounded Transitional/Strict classic DrawingML chart graph covers all 16 plot families, series/source formulas and cache metadata without retaining point values, axes/cross-axis validation, `externalData`, embedded packages and related parts. The saved-package SmartArt graph validates diagram data, point/connection topology, layout/style/color relationships and persisted drawings without retaining point text. The live drawing-layout inspector separately projects up to 128 semantic SmartArt nodes and 256 Word-associated shapes. The guarded live prepare/apply pair binds one-time node tokens to the complete Word root identity/structure/text context, replaces up to 32 single-line node texts in one Undo record and requires exact readback plus unchanged untargeted nodes. A real five-node fixture proves that Word synchronized both DiagramML data and persisted drawing parts and kept the five rendered boxes stable. A bounded active-content graph types legacy/ISO OLE declarations, linked/embedded target modes, embedded-package metadata and ActiveX topology. Office 2016 extended charts, durable DiagramML-to-runtime joins, structural/layout/style/color SmartArt mutation and active-content binary internals remain outside coverage | Safe chart edit/workbook synchronization, extended charts, SmartArt create/delete/reorder/hierarchy and layout/style/color mutation with durable identity, OLE/package binary parsers behind extraction policy, broader rendering corpus and deeper security analysis |
| Citations/bibliography | Partial (typed read graph) | Open XML 2006 and legacy Word 2004/10 `Sources` collections in Custom XML project into source-linked collections, stable sources, style/version/URI metadata, typed Tag/SourceType/GUID/LCID/scalar fields and contributor/person/corporate structure. Duplicate tags/GUIDs, invalid LCIDs/types, malformed XML and extensions remain diagnostics. Unique case-insensitive CITATION tags resolve to source nodes in the unified dependency graph. Lazy inspection is paged, redacted by default and never evaluates fields or executes XSLT | Source-type-specific required-field validation, bibliography style/locale rendering, typed add/update/delete, reference refresh and multi-version Word round-trip corpus |
| Templates/mail merge | Partial metadata foundation | Settings graph exposes bounded mail-merge mode, destination, SQL/source relationship references and redacted connection/query fields; historical generation remains separate | Typed slots/regions/constraints/data validation, relationship-type validation and repeatable execution |
| Macros/signatures/protection/encryption | Partial metadata/policy foundation (strengthened) | Typed read-only metadata inventories VBA project/data/support parts, customization payloads, VBA project-signature parts and OPC signature origin/signature topology without decoding, executing or claiming trust; settings distinguish protection metadata from encryption. `inspect_ooxml_active_content` uses independent redactions and never opens Word, while generic patch apply requires separate active-content/signature-invalidation authorizations | Safe VBA static-analysis adapter, cryptographic signature verification/removal/resign workflow, protected-operation enforcement, encrypted-package adapter and safe handoff |
| Accessibility | Partial (strengthened figure evidence) | Source-linked rules report a first heading below level one, skipped heading levels, DrawingML/VML objects without bounded title/description metadata, multi-row tables without a repeating first-row header and a missing core document title. The typed figure graph separately preserves bounded name/title/description, hidden/decorative evidence and truncation | Language, reading order, link text, contrast, grouped/decorative-object semantics, merged-table headers, false-positive calibration and Word Accessibility Checker comparison corpus |
| OCR | Not started | None | Pluggable OCR with provenance, confidence, language and privacy controls |

## Rendering and conversion

| Requirement | State | Current evidence | Exit condition |
|---|---|---|---|
| Desktop Word authoritative backend | Partial | Existing live COM save/validate/PDF and real-Word acceptance | Document-engine adapter with version/font/layout capability records |
| LibreOffice backend | Not started in new engine | Historical renderer | Isolated adapter, version probe and shared visual corpus |
| Semantic HTML/SVG preview | Partial (HTML plus exact selection and first SVG backend) | `wordtoolkit.render_ooxml_semantic_html/1.0` projects the main body, all text stories or one fingerprint-bound semantic subtree to deterministic self-contained HTML, including valid synthetic table context. `wordtoolkit.render_ooxml_semantic_svg/1.0` requires an exact target and package fingerprint, then emits a deterministic self-contained static SVG with real selectable text, `title`/`desc`/ARIA groups and estimated flow/table geometry through the same Engine/CLI/MCP package-context, selection and create-new boundaries. Links are inert, field instructions are suppressed, scripts/events/`foreignObject`/external resources are absent, standalone nonvisual/unsupported targets fail closed, and the closed result fixes pagination, exact text metrics and pixel-equivalence claims to false. Both artifacts are bounded to 256 MiB; SVG additionally fails before final XML materialization at 40,000 text lines, 100,000 generated elements or a 1,000,000-pixel canvas dimension. Response metadata returns hashes/counts rather than document text or raw XML | Add structural math/list SVG, effective-format/CSS coverage, qualified font-aware geometry, browser snapshots, selection-to-PNG/PDF and page-layout backends, sanitizer/property fuzzing and a shared cross-backend visual corpus without weakening the non-authoritative label |
| PDF/image rendering | Partial outside new core | Existing Word PDF export and historical Poppler path | Unified backend interface and object/page visual regression |
| Optional commercial/editor adapters | Not started | Research only | Licensed adapters with isolated capability and benchmark records |
| Layout diagnostics and visual diff | Partial outside new core | Existing live layout diagnostics | Page/object geometry, raster deltas, font inventory and reproducible environment |

## Extensibility and operations

| Requirement | State | Current evidence | Exit condition |
|---|---|---|---|
| Plugin architecture | Not started | Architecture interfaces listed | Versioned registrations, permissions, resource limits and compatibility tests |
| Storage/cloud adapters | Not started | Local file/stream only | Flat OPC, Graph/Drive versioned upload, cache and remote authorization boundaries |
| Capability negotiation | Partial (versioned discovery slice strengthened) | `get_wordtoolkit_capabilities` and the native `capabilities` CLI return one schema-versioned, hashed, paged contract for 109 actions without opening Word; semantic query, semantic HTML/SVG rendering, semantic-style plan/apply, comment-body plan/apply, safe formatter plan/apply, the Word-executed drawing-layout inspector, guarded SmartArt text prepare/apply pair, native caption/TOF/TOC insertion, native authority-citation/TOA insertion, native index-entry/index insertion, guarded reference-table update and saved-package patch rollback plan/apply have explicit operation versions, output schemas, normalized permission and reversibility records in their inspected action contracts, while the closed v1 summary keeps its original shape; Word COM member capabilities remain separate | Migrate the remaining 87 actions and add a unified backend/document/feature matrix with runtime probes |
| Public SDK/CLI/MCP operation convergence | Partial (ninth vertical slice) | Inspection, transform, semantic query, semantic HTML rendering, semantic SVG target rendering, semantic-style plan/apply, comment-body plan/apply and saved-package patch rollback plan/apply own typed request/result/error contracts in `WordToolkit.Engine`. `PatchRollbackWordPackageOperation` now derives the reverse, binds destination identity, evaluates semantic/risk/type/schema evidence and publishes atomically for direct .NET, the strict `patch-rollback-package` JSON CLI and lazy MCP through one parser and result projection; the Native-only reverse branch is gone. The renderers share backend metadata, package preparation, exact target selection and create-new publication without opening Word or returning document text in the response; HTML-only fragment wrappers remain isolated from SVG. Comment edits retain their invariant-checked, schema-validated atomic path. Transform still provides replace-first-text, accept-all and reject-all and implements the neutral protocol-v1 benchmark adapter | Migrate remaining operations behind the same application boundary and generate language packages from canonical contracts |
| Telemetry | Not started in engine | Existing runtime performance fields only | Privacy-safe sinks, opt-in controls, retention and failure diagnostics |
| Observability/audit log | Not started | Mutation results only | Correlation, command evidence, hashes without content, recovery references |

## Exact native Word/OOXML coverage ledger

This table mirrors every structure named in the goal. `Opaque` means its bytes survive;
it does not mean the engine understands or can edit it.

| Explicit structure | State in new engine | What remains |
|---|---|---|
| package relationships | Implemented (strengthened initial slice) | Format-specific relationship constraints, signature transforms, repair and broad corpus proof |
| content types | Implemented (initial) | MIME/parameter validation and repair |
| `document.xml` | Partial (strengthened) | Main part is projected through the lossless byte-span model and supports guarded leaf-text splice; full typed vocabulary remains |
| `styles.xml` | Partial (typed read graph + create/clone/exact-consolidate/proven-unused-delete/primary-name-rename/assignment mutation) | Defaults, latent metadata, four style types, inheritance diagnostics, theme integration and modeled effective formatting exist. Typed commands insert minimal inherited custom definitions, clone existing definitions with opaque extension preservation, remove an explicit exactly equivalent custom source after modeled reference rewrites, delete an explicit custom non-default definition after complete consumer proof, or change only a custom style's primary name while retaining its stable ID/aliases/formatting/references, then assign styles in the same atomic transaction without reserializing unrelated bytes | Arbitrary property authoring, stable-ID change/referenced-or-built-in deletion/fuzzy repair, automatic linked-style maintenance, `stylesWithEffects` mirroring, conditional table behavior and template alignment |
| `numbering.xml` | Partial (typed read graph) | Picture/abstract/instance/level/override/style-link graph and one-level effective resolution exist; sequential counters, mutations and repair remain |
| theme | Partial (typed read graph + effective resolution) | Exact relationship/content/root validation; all twelve color slots; system fallback, transform and environment diagnostics; major/minor primary and supplemental fonts; `themeFontLang`-driven explicit/likely script selection; format-scheme inventory; compact MCP views; deterministic theme font/RGB provenance in effective formatting | Exhaustive Word locale/version substitution behavior, full DrawingML color transform evaluation, exact Word quantization, mutation and visual proof |
| settings | Partial (typed read graph) | Exact relationship/content/root validation; bounded view/zoom/defaults, `themeFontLang`, compatibility profile and derived mode, legacy switches, protection metadata, document variables, attached-template and mail-merge references, separators and root inventory; compact redacted MCP views | Broader settings vocabulary, relationship-specific validation, typed mutations, version behavior and Word round-trip proof |
| fontTable | Partial (typed read graph) | Exact relationship/content/root validation; font identity/classification/PANOSE/signature metadata; four embedded faces; content-type/key readability checks; duplicate/orphan diagnostics; compact byte-free MCP views and effective-format cross-reference | Font substitution/fallback engine, obfuscation/deobfuscation workflow, mutation, licensing policy and measured render portability |
| comments | Partial (typed read graph + body-only mutation) | Standard bodies/IDs/authorship, story-scoped complete/point/damaged anchors, text counts/fingerprints/source links and public stable-comment-ID plan/apply for exact ordinary-paragraph body text across adjacent runs with privacy, explicit structural-boundary refusal, metadata-invariant, schema and atomic-write gates | Comment creation/deletion, empty/rich structural body authoring, resolution and collaboration-state mutation remain |
| commentsExtended | Partial (typed read graph) | Last-paragraph `paraId` joins, reply/root links, done state, durable-ID joins, extensible UTC/placeholder/extensions/reaction inventory and bounded corruption diagnostics; write/version compatibility remains |
| revisions | Partial (typed read graph + bounded mutation) | Authorship/date/text/property/nesting/source links span insertion/deletion/move/conflict/property/cell/custom-XML kinds; selective lossless decisions cover supported wrappers, complete moves, property snapshots, numbering-change acceptance, inserted rows, cell-insertion acceptance and cell-deletion rejection with exact inverse; paragraph merges, table-grid/vertical-merge/numbering reconstruction, custom XML and full vocabulary remain blocked |
| tracked changes | Partial (typed read graph + bounded mutation) | Revision inventory, status, named move pairs and tracking settings feed fingerprint-guarded plan/apply by stable revision ID or redacted author fingerprint; nested/move cascades are explicit and Microsoft SDK candidate validation is baseline-aware; merge and unsupported structural decisions remain |
| bookmarks | Partial (typed read graph) | Start/end markers are source-linked; ranges pair by `w:id` per story across paragraphs; duplicate case-insensitive names, missing/orphan ends and table-column ranges are diagnosed; safe edits remain |
| hyperlinks | Partial (strengthened read graph) | Semantic element node/relationship ID plus local/external HYPERLINK-field dependency edges; element and field forms are not yet unified and edits remain |
| fields | Partial (typed read graph) | Nested complex begin/separate/end parser, recursive `fldSimple`, bounded tokenizer, field-family classification, source links, parent/child graph, cached-result bounds and dependency edges; evaluator/update/safe edits missing |
| TOC | Partial (guarded native creation and refresh) | TOC/TC fields and `\\b` bookmark dependencies are recognized. Live Word can create one heading/outline-based TOC from levels 1–9, choose page-number/hyperlink options, repaginate, update and reacquire the native field range without exposing instructions or result text; existing TOCs can be refreshed by exact index or with all reference tables | TC identifiers, additional-style mappings, chapter separators, safe saved-package planning, multi-version/locale corpus and renderer-neutral layout semantics remain |
| footnotes | Partial | Story container/items/references and lossless text edits; separator and numbering semantics missing |
| endnotes | Partial | Story container/items/references and lossless text edits; separator and numbering semantics missing |
| OfficeMath / OMML | Partial (typed read graph + bounded live verification) | All standard object families, roles, normalized properties, source anchors, settings, display/inline placement and malformed/extension diagnostics are modeled and paged; raw OMML and formula text are hidden by default. Live sensitive equations are read back under XML bounds and checked by canonical hash, symbol count, integral-owned differential ancestry and semantic style/control placement without returning OMML. Script, Fraktur, double-struck, sans-serif and monospace Latin runs are reconstructed from native `m:scr` properties. LaTeX, MathML and OMML now author and verify all four native `m:sty` values plus separate `m:ctrlPr/w:rPr` control bold/italic, with documented default normalization and no exposed build markers | Unified cross-format semantic algebra, serializers, mutation/repair, equation numbering integration, mathematical equivalence, contextual Arabic MathML variants and broader Word-version round-trip/visual proof |
| sections | Partial | Source-linked boundaries, break/page/margin/column/numbering properties and header/footer inheritance graph; structural edits missing |
| headers | Partial (strengthened) | Related parts, text edits and effective default/first/even bindings across sections; link mutation and layout missing |
| footers | Partial (strengthened) | Related parts, text edits and effective default/first/even bindings across sections; link mutation and layout missing |
| page layout | Partial (live drawing-object slice) | Section/page properties and paragraph-flow diagnostics remain, while the connected-Word drawing inspector can force repagination and read page/section placement plus conditional page-relative boxes for drawing objects; no complete off-screen page tree, line boxes or renderer-neutral pagination model |
| page breaks | Partial | Generic break nodes; break kind and pagination semantics missing |
| tables | Partial (typed read graph) | Source-linked logical grid, row skips, `gridSpan`, exact-span `vMerge`, legacy `hMerge` inventory, nested tables, header contiguity, row exceptions, widths/layout and bounded redacted MCP views; structural edits and full conditional-style/render behavior remain |
| floating tables | Partial (typed read graph) | Declared and Word-effective `tblpPr`, Word anchor defaults, ignored story/default cases, coordinate range diagnostics and independent layout opt-in; wrap/render mutation and licensed version proof remain |
| floating images | Partial (declared + live Word placement) | Package declarations retain extents, distances, reference frames, stacking and wrap polygons; connected Word returns anchor, page/section, reference-aware offset/alignment, wrap, size, rotation and conditional page-relative bounds. Safe mutation, line-level collision flow, durable package/runtime identity and multi-version proof remain |
| DrawingML | Partial (typed package graph + live object layout) | Figure resources/placement, bounded group/shape topology, transforms, preset/custom paths, line/fill/effect kinds and text-flow declarations plus all 16 classic chart families are typed. Connected Word separately executes pagination and projects runtime shapes/groups with explicit coordinate spaces; formula/effect/theme evaluation, mutation, off-screen page geometry and durable declaration/runtime joins remain incomplete |
| SmartArt | Partial (native structure graph + live node/shape projection + guarded text mutation) | Diagram data, points, connections, layout/colors/styles and persisted-drawing relationships are validated without retaining point text; connected Word can expose bounded semantic nodes and associated runtime shapes. One-time whole-root-bound tokens now permit up to 32 single-line node-text replacements through Word with exact readback and rollback. A real five-node package proves synchronized `data1.xml`/persisted `drawing1.xml`, zero SDK errors and stable rendered box geometry. Durable DiagramML/runtime node identity, create/delete/reorder/hierarchy, layout/style/color mutation and cross-version layout equivalence remain |
| charts | Partial (classic typed graph) | All 16 classic plot families, series/formulas/cache metadata, axes/cross-axis links and related parts; cache values, extended charts and safe edits remain |
| diagrams | Partial (native topology + live Word projection + guarded node-text mutation) | Stable package diagram/point nodes, typed connections and part dependencies plus bounded live SmartArt node/shape layout and whole-root-verified single-line text replacement through Word; no durable cross-model identity, structural/layout mutation or renderer-independent output |
| embedded Excel | Opaque only | Safe inventory/extraction, workbook linkage and policy |
| embedded Visio | Opaque only | Safe inventory/extraction, relationships and rendering |
| OLE objects | Partial (typed metadata graph) | Legacy/ISO declarations, internal/external target mode, payload metadata, dependency edges and redacted inspection; compound-file internals are never decoded | Extraction policy, compound-file parser, preview, mutation and security analysis |
| VBA | Partial (typed metadata graph) | VBA project/data/support/signature part and relationship inventory, macro-container consistency and redacted inspection; never executed | Static analysis, signature trust/policy and authorized mutation |
| custom XML | Partial (typed read graph) | Relationship-discovered data/property items, GUID/schema/root metadata, malformed-store retention, built-in property stores, restricted binding targets, typed bibliography collections/sources and unified dependencies; general Custom XML values never leave the content-control inspector, while bibliography values require a separate explicit sensitive-data opt-in | Lossless value/schema edits, binding refresh, template planning, merge and broader Word-version proof |
| custom properties | Partial (typed read graph) | Exact relationship/content/root validation; Transitional/Strict custom namespaces; typed scalar/complex classification; `pid`/`fmtid`, duplicate-name/ID and lexical diagnostics; privacy-redacted bounded inspection; unique valid scalar values resolve `DOCPROPERTY` dependencies | Typed create/update/delete/renumber transaction, complex-value decode policy, serializer and Word round-trip proof |
| document variables | Partial (typed read graph + dependencies) | Bounded settings variable names and values; names/values hidden by default in MCP; unique persistent variables become dependency nodes and resolve `DOCVARIABLE` reads while `SET`/`ASK` remain field-local | Typed mutation, policy, targeted disclosure and Word round-trip proof |
| glossary | Partial | Glossary story and building-block names/GUIDs are projected and text-editable; typed insertion and gallery/category behavior missing |
| content controls / SDT | Partial (typed read graph) | Stable semantic/source IDs, parent topology, 18 type classes, block/run/row/cell level, alias/tag/native ID, locks, placeholders, temporary state, standard/w15 bindings, repeating sections/items and bounded redacted MCP views | Typed edits, list/date/checkbox option details, display refresh, rich-text binding decode/write and Word-version behavior |
| mail merge | Partial (metadata read graph) | Settings destination/mode and relationship references plus redacted query/connection metadata | Field/data-source graph, relationship validation, regions, policy and deterministic execution |
| macros | Partial (typed metadata graph) | VBA/support/customization inventory and macro-container consistency; no execution or binary analysis surface | Static analysis, policy decisions and authorized mutation |
| signatures | Partial (typed topology only) | Package origin/signature and VBA project-signature parts/relationships are inventoried; no cryptographic validity claim | Trust-chain/timestamp/revocation validation, explicit invalidation/removal and resign workflow |
| encryption | Not started | Encrypted OOXML detection, authorized decrypt/encrypt adapter |
| permissions | Partial (typed read graph) | Story-scoped `permStart`/`permEnd` pairing with editor/group and table-column scope plus orphan/duplicate/reversed diagnostics | Mutation enforcement, authorization policy and Word-version proof |
| protection | Partial (metadata/policy only) | Document/write-protection modes and algorithm metadata are typed; secrets never returned; editing restriction is explicitly not treated as encryption | Permission-range integration, mutation enforcement, password workflow, authorized encryption adapter and Word probes |
| revision IDs | Partial (strengthened) | Stable graph IDs, native `w:id` values, nested parents and move-range links are distinct from comment paragraph/durable IDs; duplicates and unresolved links are diagnosed | Cross-document/version identity, merge collision policy and mutation-safe ID allocation |
| style inheritance | Partial | Base-first `basedOn` graph, default selection, link diagnostics, modeled property provenance and deterministic theme dereferencing exist; assignment rejects unresolved target chains, while definition mutation and conditional table/version behavior remain |
| numbering inheritance | Partial | Abstract/instance/full-level/start override and numbering-style indirection resolve with provenance; paragraph-sequence counters, restart execution and edits remain |
| XML namespaces | Partial (strengthened) | Prefixes, declaration placement, expanded element/attribute names and untouched bytes are retained; general namespace-changing edits remain |
| compatibility mode | Partial (typed read graph) | Bounded `compatSetting` tuples, legacy switches and explicit derived `compatibilityMode` with duplicate/conflict diagnostics | Versioned behavioral profiles, broader setting interpretation and Word probes |
| Word version differences | Not started | Versioned capability profiles and corpus |
| co-authoring metadata | Partial read-only | People author/provider/user records, comment thread/durable/extension/reaction inventory and revision author links; collaboration sessions, live presence, change logs and merge semantics remain opaque |

## Exact semantic-operation ledger

These are not aliases for string replacement. Each requires a typed selector, plan,
preconditions, affected-node proof, transaction, validation, and inverse.

| Operation named in the goal | State |
|---|---|
| replace every definition with style `Definition` | Partial: one transaction can create or clone `Definition`, server-select paragraphs by strict text/property/structural evidence and assign the same-plan-created style under exact fingerprint/plan preconditions without echoing node IDs; linguistic classification of what constitutes a definition is not implemented |
| find every theorem | Not started |
| rewrite only the paragraph containing an equation | Not started |
| change table style from APA to IEEE | Partial: a minimal table style can be created or an existing table definition cloned, then exact or server-selected tables can be reassigned in the same plan; APA/IEEE intent inference, arbitrary table-style property authoring and conditional rendering are not implemented |
| create a table of figures | Implemented through guarded live Word insertion and a separate guarded refresh path; installed Word 16.0 creation and three-kind refresh DOCX/PDF round trips pass, while multi-version/locale coverage remains open |
| repair numbering | Not started |
| repair styles | Partial: explicit `consolidate_style` repairs only a proven canonical-equivalent custom duplicate under exact source/target IDs; explicit `delete_unused_style` removes only a custom non-default style after proving no surviving consumer; explicit `rename_style` changes only the primary visible name while preserving the stable ID, aliases, formatting and ID-based references. All use strict blockers, one predicted fingerprint, SDK candidate validation and an exact inverse. Fuzzy equivalence, automatic ranking/linter binding, stable-ID changes, referenced/built-in deletion and template alignment remain |
| repair references | Partial: existing native tables of contents, figures and authorities can be repaginated and fully updated together or by exact kind/index under version, 128-object, Undo, stable-collection and range/field readback gates. Broken dependency diagnosis and saved-package repair remain missing |
| repair footnotes | Not started |
| repair XML relationships | Diagnostics partial; repair not started |
| repair a corrupted document | Detection partial; repair not started |
| detect unused styles | Partial (strengthened): the dependency-backed native rule excludes defaults and styles referenced by semantics, numbering, inheritance or links. Its finding is not deletion authorization. A separate explicit `delete_unused_style` plan re-checks stricter story/revision/style/numbering/glossary/latent/`STYLEREF` and unmodeled-XML consumers before mutation |
| detect duplicate styles | Partial (strengthened): the native rule groups styles whose fully modeled declared formatting is equivalent and the semantic planner can safely consolidate an explicitly chosen pair only under stricter full canonical equivalence. Automatic linter-to-repair pairing, ranking and fuzzy identity policy remain unresolved |
| detect dead relationships | Partial (strengthened): `OPC040` plus source-linked dependency part reachability and unresolved relationship edges; typed repair not started |
| minimize package size | Partial (strengthened): exact custom-style consolidation removes redundant definitions and rewrites their modeled consumers; strict unused-style deletion removes explicitly selected dead custom definitions with preservation proof. Dead parts, images, fonts, ZIP policy and automatic candidate selection remain unimplemented |
| rebuild numbering | Not started |
| repair OfficeMath | Not started |
| rewrite comment bodies only | Partial (public bounded implementation): stable `comment_id`, cross-run exact replacement, expected match/body preconditions, deterministic plan/apply, metadata and unselected-body invariants, SDK validation, atomic backup, no text/XML response; empty comments and structural rich-body rewrites remain |
| accept only changes by author X | Partial: redacted author-fingerprint selection, explicit dependency cascade, deterministic plan/apply, exact inverse and atomic backup cover supported wrappers/moves/property snapshots/inserted rows plus proven one-sided cell/numbering decisions; unsupported paragraph/table-grid/vertical-merge/numbering/custom-XML structures block |
| revert changes by author Y | Partial: the same saved-package planner rejects the selected author's supported revisions under exact fingerprint/plan preconditions; unsupported dependencies block and can be routed to guarded live Word |
| align styles with a template | Not started |
| compare two documents | Partial: lazy `compare_ooxml_semantics` separates package and semantic verdicts, compares every projected Word story without opening Word, pages redacted differences, exposes match confidence/ambiguity/fallbacks and preserves opaque changes as explicit entry evidence; it does not yet create a tracked-change document or visual diff |
| create a patch | Partial: deterministic portable `.wtpatch` stores exact before/after uncompressed OPC entry payloads, hashes and reversible operations under exact fingerprints; a semantic-command patch and byte-identical ZIP-container replay remain future work |
| create a merge | Partial: explicit three-way plan/apply creates a new package, auto-composes proven disjoint lossless text edits and requires stable explicit resolutions for every other entry/text conflict; structural and revision-aware merge remain future work |
| render to HTML | Partial: whole main body, all projected text stories or one exact fingerprint-bound subtree render to deterministic, self-contained, non-paginated semantic HTML with inert links, revision annotations, field-instruction suppression, linear equation fallbacks, explicit placeholders, valid selected table-fragment contexts and an atomic create-new artifact contract; page/layout fidelity and richer formatting remain |
| render to SVG | Partial: one exact fingerprint-bound semantic subtree renders through the native Engine/CLI/MCP backend to deterministic static SVG with real text, accessible title/description/groups, estimated flow/table geometry, inert links, suppressed field instructions and explicit non-pagination/non-pixel-fidelity metadata; structural OfficeMath/DrawingML geometry, exact fonts/layout and whole-page rendering remain |
| render to PNG | Not started in new engine |
| render to PDF | Not started in new engine |
| render one page | Not started |
| render only a table | Partial: exact table/row/cell subtree selection works in semantic HTML and static SVG; SVG grid sizes are estimated and merged/nested geometry is warned or flattened |
| render one equation | Partial: exact equation subtree selection works in semantic HTML and SVG as bounded linear text; structural glyph/layout rendering remains |
| generate a document AST | Partial read-only semantic AST |
| generate a dependency graph | Partial: one bounded saved-package graph joins OPC, semantic containment, styles, numbering, references, bibliography collections/sources/resolved CITATION tags, resolved category-matched `TA` authority entries and `TOA` field nodes including category-zero all-category tables, sections, classic charts, SmartArt diagrams/points/connections/parts, logical figures/representations/resources with declared DrawingML/VML placement, nested figure-shape topology, captions and selected/candidate associations, content controls, physical/built-in XML stores, binding targets, repeating-section items, nested tables, vertical-merge continuation cells, document properties/variables and active-content payload/declaration/ActiveX topology. Word-executed drawing objects now have a separate bounded live projection, but traversal-scoped runtime identities are not injected into the durable graph; active-content internals, signature cryptography, encryption and collaboration remain outside the typed spine |
| generate a style map | Partial typed, paged graph plus filtered paragraph/run effective-property slice and provenance |
| generate a section structure | Partial typed boundary and effective header/footer binding graph |
| generate a document analysis | Package/semantic counts partial; full analysis not started |

## Exact AI object-model ledger

| Required high-level object | State |
|---|---|
| Paragraph | Partial read-only |
| Run | Partial read-only |
| Equation | Partial canonical OfficeMath read object; cross-format algebra and edits missing |
| Heading | Not typed; style metadata only |
| Section | Partial read-only boundary, page properties and story bindings |
| Table | Partial read-only: stable source-linked table/row/cell objects now expose logical grids, merge chains, nesting, headers, layout and floating state; structural mutation/rendering remain |
| Figure | Partial read-only: stable package figure/representation/placement/accessibility/resource and bounded declared shape topology across Transitional/Strict DrawingML, VML and legacy objects, complemented by traversal-scoped Word-executed floating/inline/group layout. AlternateContent selection is not MCE-evaluated; durable cross-model identity, mutation, full effect evaluation and render output remain missing |
| Bookmark | Partial paired, source-linked read object; no safe range edits |
| Reference | Partial typed dependency edge across fields/bookmarks and named targets |
| Caption | Partial read/write: source-linked caption-style/SEQ evidence, label/result metadata and conservative nearby-figure candidates remain; guarded live Word can insert one localized native caption, generate one native table of figures and refresh existing contents/figures/authorities/indexes with version, Undo and readback gates. Renumbering policy, figure replacement and durable saved-package mutation remain missing |
| Authority citation/table | Partial read/write: guarded live Word marks one token-bound native `TA` entry and creates one category-specific or all-category native `TOA` with semantic separators/leaders, exact native option readback, one Undo record and rollback. Saved-package reference/dependency graphs resolve complete category-compatible `TA` entries to concrete `TOA` field nodes. Existing-entry edit/removal, category-definition management, legal-style normalization and durable saved-package mutation remain missing |
| Index entry/index | Partial read/write: guarded live Word marks one token-bound native `XE` entry with explicit hierarchy, optional cross-reference or existing-bookmark page range and page-number emphasis, then creates one native `INDEX` with semantic heading/type/column/accent/leader options, exact native readback, one Undo record and rollback. Saved-package reference/dependency graphs resolve complete `XE` entries to concrete complete `INDEX` field nodes. Mark-all/concordance files, AutoText, phonetic reading/language/filter/sort/format policy, entry edit/removal and durable saved-package mutation remain missing |
| Comment | Partial source-linked body/anchor/thread/durable/person/reaction object plus public body-only text transaction; creation/deletion, rich structure and full collaboration semantics missing |
| Field | Partial nested complex/simple read graph; no evaluator or safe edits |
| Style | Partial typed graph, defaults, metadata, declared properties, inheritance diagnostics and modeled effective paragraph/run properties |
| Numbering | Partial read-only graph and effective level; no sequence counter or structural edits |
| Footnote | Partial body/reference object; numbering semantics missing |
| Image | Partial read-only resource relationship and accessibility/placement metadata; bytes/pixels and mutation missing |
| Shape | Partial typed read model: stable nested groups/shapes, transforms, preset/custom path commands, formula points, fill/line/effect kinds and text-flow declarations; guide/effect/theme evaluation, rendered layout and mutation missing |
| Chart | Partial typed classic chart graph plus figure-resource declaration; extended charts and mutation missing |

AI currently receives package summaries, bounded semantic nodes and redacted typed
field/bookmark/reference, figure/caption, OfficeMath and review graphs. It now also receives deterministic
text and tracked-review plans and can execute those bounded commands without raw XML; it
still lacks the complete object model, general query/planning language and broad typed
mutation/repair/render execution required by the goal.

## Proof, performance, and release gates

| Requirement | State | Current evidence | Exit condition |
|---|---|---|---|
| Unit/regression tests | Partial | 510 engine, 395 native and 1309 Python tests pass at the current checkpoint, with 16 intentional Python skips. Operation-wide rollback tests cover exact restoration, visible-state-only restoration with hidden OOXML residue, supplemental-state mismatch, partial/no-op/false/throwing Undo, failed custom-record closure, quarantine, reconnect blocking/release, legacy-entry-point removal, isolated equation staging failure, failed staging-open artifact cleanup, hidden pre-publication target drift, failure before target mutation, one-call heterogeneous publication with zero target-side OMath builds, accepted independent recovery after `Undo=false` and semantic hashing that ignores only `w:rsid*` session attributes. Saved-package rollback tests cover exact destination-bound reverse restoration, redo-backup contents, stale result fingerprints, cross-path plan rejection, active-content authorization, no-op non-mutation, validator absence, closed JSON, privacy bounds, public contracts and canonical result parity across direct SDK, strict CLI and MCP. Formatter tests cover exact scalar removal; exact full-group removal for fonts, color, underline and shading; inherited theme/fallback restoration; independent scalar progress beside a rejected group; the 64-proof fail-closed ceiling; conditional-table/revision/unmodeled skips; inverse bytes; stable no-op; stale fingerprints; strict request bounds; signature blocking; destination/plan drift; source preservation and installed-runtime create-new behavior. Drawing/SmartArt tests cover floating/inline/group projection, privacy, closed schemas, guarded mutation, external context drift, exact-readback rollback and stable no-op. Forced real Word proof publishes two formatted text operations plus the eight-line integral derivation through one staged batch and re-verifies native math semantics; rollback proof separately shows both `Undo=true` with changed hidden state and independent Flat OPC recovery that restores visible text/OMath but not semantic whole-package equality, with mandatory quarantine in both unproven cases | Coverage for every required feature and published failure corpus |
| Property/fuzz testing | Partial | Deterministic malformed bytes and random opaque round-trip smoke | Continuous coverage-guided fuzzing, minimized corpus and resource assertions |
| Fault injection | Partial | The shared live rollback lane injects no-op, partial, visible-only, false and throwing Undo, supplemental-state drift, custom-record closure failure, isolated staging rejection, hidden target drift during staging, publication rejection before mutation, partial publication failure and both successful and failed independent baseline restoration. Every current custom-Undo live mutation family uses that verifier; `apply_live_word_operations` additionally owns the independent recovery path. Real Word independently reproduced `Undo=false` with 33 residual paragraphs, `Undo=true` with restored visible structure but changed Flat OPC/range/story hashes, and recovery from a Flat OPC clone that removed text/OMath residue while still changing the semantic package hash. Every unproven state produces `ROLLBACK_FAILED` plus handle/document quarantine | Per-family injected failures beyond the shared verifier, disk-full, denied, staging-cleanup failure, process/Word crash, full-package recovery/swap failures and race tests |
| Preservation benchmark | Partial | Entry hashes and random no-op round trip | Public producer/feature corpus with untouched part/subtree metrics |
| Performance benchmark | Partial | The reproducible benchmark harness now includes a checked-in raw 10,000-source bibliography result with one person and one matching `CITATION` per source. Seven same-process Release samples resolve 10,000/10,000 citations with zero issues and preserve the package fingerprint; bibliography projection is 642.5681 ms median and 811.9211 ms p95/max, with 245,631,072 median thread-allocated bytes and 81,230,184 of 671,088,640 operation-accounted bytes. There is no before/after delta because the previous HEAD had no bibliography graph. Five cold-process 0.39 samples keep the 99,997-node dependency result at a deterministic 539,282,576 of 671,088,640 operation-accounted bytes and 130,132,744 graph-accounted bytes. Median dependency build is 2,956.082 ms and total measured time 5,491.3294 ms; versus the same-host 0.38 series this is +0.07% and +1.08%. Median retained managed delta is 175,334,544 bytes (-10.26%), allocation delta 1,610,477,144 bytes (+0.11%) and peak working set 615,305,216 bytes (+6.75%), so no peak-memory improvement is claimed. All five raw 0.39 samples and the prior 0.37/0.38 series are checked in. The checked-in semantic-SVG point projects 9,996 nodes from a requested 10,000-node package, selects a six-node table and emits the same 1,305-byte artifact and SHA-256 seven times at 449.10 ms median and 844.87 ms p95/max, with no source mutation, Word process, external resources or active content. Whole-package projection still dominates, so this is deterministic bounded-output evidence rather than Word-layout or proportional selection-latency evidence. The preceding HTML semantic-selection point rendered the same six-node target to 3,074 bytes versus 541,043 bytes for the full artifact (0.5682%), also with equal repeated hashes and bytes. The preceding packaged whole-document operation rendered the checked-in Mammoth fixture to one deterministic 3,628-byte artifact across 15 cold processes at 254.66 ms median and 290.44 ms p95/max. Checked-in Windows x64 dependency, content-control binding, table, `.wtpatch` materialization and source-preserving MCE points report elapsed time, retained memory, allocations and peak working set. The packaged comment-body planner completed 15 cold process runs at 669.53 ms median and 688.29 ms p95/max with a fixed 1,027-character result, no source mutation and no Word process. The remote copy-on-write mutation point reports 15 one-paragraph samples: 0.160 ms direct median versus 56.319 ms for snapshot/clone/mutate/snapshot/structural-validate (+56.159 ms, 351.99x), explicitly a correctness cost; engine creation, commit/cleanup, MCP dispatch and the unavailable SDK validator are excluded. The new 15-sample heterogeneous remote batch point executes format, revision settings and paragraph insertion through FastMCP in 70.901 ms median versus 189.479 ms for three standalone COW calls (-62.58%), with one rather than three clone commits; creation/close and the unavailable SDK validator are excluded. The binding graph resolves 10,000 objects under production limits and reaches its 100,000-control/binding ceiling only with a disclosed 64 MiB benchmark metadata budget; that point takes 15.40 seconds, peaks at 1,761.8 MiB working set and allocates 5,960.7 MiB. The 100,000-cell table point takes 5.23 seconds, peaks at 579.3 MiB and allocates 1,885.1 MiB. The historical integrated 998,998-node dependency point took 40.08 seconds and peaked at 5,209.2 MiB under an explicit high-limit path that the production budgets reject. The largest MCE point retains about 1.03 GB managed memory. These are explicitly hard-boundary evidence, not ordinary workloads | Shared immutable parsed-story storage, broader parse/edit/save/render corpus, denser cross-domain graphs, repeated-process stability and qualified cross-host results |
| AI token benchmark | Partial (strengthened) | Lazy catalogue and bounded responses; earlier 83.5% schema reduction; bulk style assignment can keep up to 200 resolved node IDs out of model input by using one server-side selector with an explicit match ceiling. The 200-target regression fixture reduced serialized command input from 19,014 to 186 characters (99.02%, 102.23x smaller), while the earlier six-target packaged proof reduced it by 77.53%. The style-definition proof used 218 serialized command characters to clone one definition and assign it to six paragraphs; its compact seven-operation plan line was 2,146 characters with detail arrays omitted. The exact-consolidation proof used one 108-character command and a 902-character compact plan for six server-counted reference rewrites. The unused-style deletion proof used one 55-character command and a 934-character compact plan; reordered JSON properties produced the same plan ID. Both semantic action schemas remain regression-capped below 4,500 characters. The heterogeneous remote batch measured 427 compact request JSON characters versus 480 across three standalone requests (-11.04%) and returns only bounded IDs/counts/status, never inserted text. A representative one-step compact success envelope fell from 290 to 102 characters after removing repeated protocol/backend/atomicity values and request operation names. Its closed 33-variant input schema costs 19,088 compact characters even after duplicate-title/envelope removal; the complete 66-tool compact catalog rises from 57,566 to 77,439 characters (+34.52%), so no whole-catalog token reduction is claimed and a regression caps the batch schema below 20,000. These figures cover command input or named response lines, not the whole task dialogue. Field-heavy reference and dependency result data plus default equation result data are regression-capped below 5000 serialized characters. The default 18-rule lint summary is also capped below 5000 characters and its complete mirrored JSON-RPC envelope below 10000. Default chart, content-control, table and MCE data are capped below 5000 characters and their complete mirrored envelopes below 8000; the packaged real table fixture used 2,308 characters and the content-control fixture 2,718 characters for the whole tool-call line. Nested object IDs, grid widths, target/item IDs and namespace/schema arrays have independent caps. Dependency, lint, chart, content-control, table and MCE source evidence is redacted by default; cell text, raw table XML, Custom XML values and chart cache values have no response field. Compact live-equation preflight omits converted linear math and rule arrays in favor of lengths, flags and a short fingerprint; live verification returns hashes/counts but never raw OMML. The MCP compatibility envelope still mirrors successful data in both `content` and `structuredContent`; that duplication is not hidden under the smaller result-data claim | Representative task suite against competitors with raw token logs, followed by a compatibility-safe removal of duplicated payload tokens |
| Visual regression | Partial static semantic HTML/SVG plus licensed Word formatter checkpoint | Packaged whole-document HTML parses into the expected story/paragraph tree, includes CSP and contains no external resource elements. The selected Mammoth table parses with valid HTML table ancestry. Engine SVG regressions prove accessible inert output. For the final installed formatter proof, Microsoft Word validated source/result with zero SDK errors and exported one-page 23,821-byte PDFs; their 144-DPI page PNGs were byte-identical at SHA-256 `2a882af2560fb684e55664c647e964ae3eebd98403292eaf07def3463895c966`, and direct image inspection found no clipping, overlap or font/color/underline/shading drift. This is one real Word point, not a qualified broad-corpus or multi-version claim | Versioned browser snapshots plus PDF/page/object baselines across rendering backends and qualified Word versions |
| Cross-platform CI | Partial | Mandatory Linux engine job plus clean hosted-Windows engine/native/package jobs; licensed Word gate remains separate | macOS core job, qualified backend matrix and routinely available self-hosted Word release evidence |
| Public competitor benchmark | Partial (first neutral checkpoint) | Pinned `docx-platform-tests` protocol-v1 run across 42 scenarios: WordToolkit 19 pass + 2 invariant-pass + 21 unsupported; safe-docx 18 + 2 + 22; zero failures/errors/divergent passes/protocol mismatches. Exact commits, commands, caveats, raw 74,657-byte JSON and SHA-256 are checked in | Producer-diverse hostile corpus, more adapters, preservation/Word-open/visual/token/latency/security metrics and licensed-engine lanes |
| Release packaging | Partial (strengthened) | The current `0.39.0+codex.20260724165517` Windows x64 package is self-contained and contains zero Python files. The pinned .NET SDK 8.0.423 build produced a 196-file, 87,150,234-byte tree and a 36,755,923-byte ZIP at SHA-256 `c9216579cede83aec95cab4b895a30008d9c33fcfb28ba0ea3163566e937648d`; executable, runtime assembly, Engine assembly and Open XML SDK adapter SHA-256 values are `8bcb384c8f4dd4fea526c9e4c2ac81935d4542e1c7dc7436a4812d5f1dd2aabb`, `71f12542a6da3f22b3fe6e9b04234ed33448978c99a64139c2f46e1b75c61fbe`, `b0e93320b14ebe247988ef6b62d5ac5429df87b98df87fcd70d9cc509c5ddb42` and `e6ce1df006670abba3f630304f31dd4d3a6aa80fa3a9d92d2af768741f9f4125`. Build output, personal source and enabled cache contain the same 196 files with zero path/length/hash differences; a second same-checkout output tree and ZIP are byte-identical, while the separate clean-checkout reproduction remains open; installed discovery reports the exact stamped version, 109 actions, 15 exposed tools and 22 explicit metadata contracts, and the installed executable exposes the strict patch-rollback CLI. Forced Word 16.0 evidence now covers complete heterogeneous Flat OPC staging and one-call publication, `Undo=true` hidden drift, and independent baseline reconstruction after absent Undo. The last path restores text and OMath counts but still fails package-semantic proof beyond `w:rsid*`, so it quarantines. The preceding index, authority, contents-table, all-reference-table, caption, SmartArt and formatter proofs remain valid for their narrower slices. Packaging canonicalizes copied JSON/Markdown and embedded schema to BOM-less UTF-8/LF under Windows PowerShell 5.1 | Reproduce the current archive from a second clean checkout, obtain package-exact live recovery or sanctioned identity swap, add optional signing/provenance policy and routinely repeat the licensed Word release lane across qualified Office builds |

## Current checkpoint evidence

- `dotnet test native/WordToolkit.Engine.Tests` — 504 passed.
- `dotnet test native/WordToolkit.Native.Tests` — 389 passed.
- `.venv/Scripts/python -m pytest -q` — 1309 passed, 16 intentionally skipped.
- `dotnet run --project native/WordToolkit.Engine.Benchmarks -c Release --no-build -- bibliography --target-nodes 10000 --output artifacts/benchmarks/bibliography-10k-final-2.json` — seven graph samples; 10,000/10,000 citations resolved, 642.5681 ms median, 811.9211 ms p95/max, 245,631,072 median thread-allocated bytes and package fingerprint preserved.
- The current dependency-memory checkpoint keeps the 128 MiB graph-local byte boundary
  and adds one 640 MiB cumulative operation lease across OPC, every parser/projection and
  final graph construction. The 99,997-node fixture consumes 539,282,576 operation bytes
  and 130,132,744 graph bytes; a 512 MiB candidate failed during graph construction, so
  the calibrated default preserves the admitted boundary with 19.7% headroom. This is a
  deterministic allocation proxy, not an exact CLR heap or resident-memory ceiling.
  Input-schema SHA-256 remains `e371a9c3800f58dcd685c80a9d5a63cee967aa2ba563a8bb01965c373f06b7a2`.
  Five cold 0.39 processes report 2,956.082 ms median dependency build, 5,491.3294 ms
  measured total and 615,305,216-byte median peak working set. Two local builds are
  byte-identical and the enabled personal cache exactly matches the package; hosted-CI
  evidence remains pending in this working slice.
- The current Figure/Caption checkpoint exposes one typed graph through
  `inspect_ooxml_figures`, binds IDs to the package fingerprint, preserves uncertain
  MCE branches without inventing a primary representation and redacts text, source,
  targets and diagnostics by default. Two supported package builds matched exactly at
  196 files, 85,719,250 expanded bytes and ZIP SHA-256
  `addb0ac796b9c5e41e6174f2bd1937d9fe996ae88015a81af5b000968caa63c7`;
  the enabled personal-plugin cache has zero file/hash differences. The installed CLI
  reports runtime 0.37.0, 90 actions and the new lazy action. The checked-in benchmark
  drives 10,000 distinct relationship IDs and reports 1,897.6 ms median, 2,084.6 ms p95,
  317,194,760 retained managed bytes and 1,251,549,184 peak working-set bytes. The
  preceding semantic-SVG checkpoint and hosted run `29992366785` remain historical
  evidence for the unchanged exact-target renderer, not proof of current hosted CI.
- The current body-only comment checkpoint exposes the public
  `wordtoolkit.plan_ooxml_comment_body_edits/1.0` and
  `wordtoolkit.apply_ooxml_comment_body_edits/1.0` contracts through one provider-neutral
  Engine path shared by direct .NET, strict JSON CLI and lazy MCP. Two supported package
  builds matched exactly at 196 files, 85,366,594 expanded bytes and ZIP SHA-256
  `267ace7127d82c8f54c32f56682830770d6209f1880b4f00d38a31dc45aa5503`;
  their expanded manifests matched at SHA-256
  `25fd2c4afcfef292c95c5925d849cee55b5de1297dbbf0b05734e9edd9231373`.
  Packaged plan/apply on the real Mammoth fixture reproduced plan/result fingerprints,
  changed only `word/comments.xml`, retained a byte-exact backup, passed Microsoft schema
  comparison, returned neither comment text nor XML and did not start Word. Fifteen cold
  packaged plan runs measured 669.53 ms median and 688.29 ms p95/max with an invariant
  1,027-character response. Hosted CI run `29980881560` passed all five jobs; the
  downloaded Windows ZIP matched both local archives byte for byte and its expanded tree
  matched at all 196 file paths, sizes and hashes.
- The current semantic-style interoperability checkpoint moves the public plan/apply
  contract, exact candidate construction and atomic commit boundary into the
  provider-neutral Engine shared by direct .NET, JSON CLI and MCP. It adds the separate
  Open XML SDK adapter, strict bounded JSON, candidate/result identity checks, redacted
  validation failures and deterministic tests for both the final replacement race and a
  second writer during compensation. The latter survives byte-for-byte in an opaque
  `.conflict` artifact rather than being deleted. Two local packages matched exactly at
  196 files and ZIP SHA-256
  `9a3fc1ae866ca9d5e1f9c1f40b21683491175b8e63125511f4dd6529bff6596e`;
  the packaged real-DOCX plan was 1,811 characters and 15 cold runs measured 747.58 ms
  median and 779.22 ms p95 without mutating the source or opening Word. Packaged apply on
  a disposable copy matched the reviewed plan/result identities, changed only
  `word/styles.xml`, passed Microsoft schema comparison and retained a byte-exact backup.
  Mandatory CI run `29977852683` passed all five jobs; its downloaded Windows ZIP matched
  both local archives byte for byte at the same size and SHA-256.
- The current `wordtoolkit.transform_ooxml_package/1.0` checkpoint exposes replace-first
  text, accept-all and reject-all package transforms through SDK, CLI and MCP. The same
  core backs a protocol-v1 neutral benchmark adapter. Local proof covers 366 engine and
  247 native tests, canonical Engine/CLI/MCP parity, cross-run replacement, OfficeMath
  exclusion, MCE ambiguity rejection, opaque-entry preservation, exact revision inverse,
  digital-signature/output-collision blocking, stable protocol codes and no Word
  invocation. The pinned 42-scenario public harness records 19 pass + 2 invariant-pass
  for WordToolkit against 18 + 2 for safe-docx, with no failures or errors and every
  unsupported operation reported explicitly.
- The preceding `wordtoolkit.inspect_ooxml_package/1.0` checkpoint exposes the same typed,
  canonical result through SDK, CLI and MCP. Local proof covered 357 engine and 243 native
  tests, exact CLI/MCP data parity on a real fixture, two byte-identical packaged builds,
  stable error classes, read-only file hashes, no Word invocation and an independent
  red-team with no unresolved P0/P1. The packaged 6,827-character result measured
  265.965/286.932 ms CLI p50/p95 and 321.721/348.233 ms MCP p50/p95 across 20 cold runs;
  the Word-process count remained zero.
- Native MCP regression against real Word verified Gaussian, nested and double
  integrals, Presentation MathML, OMML, a parenthesized matrix, cases and combining
  accents. Every sensitive result returned matching canonical hashes; all
  integral-owned differentials remained in `m:nary/m:e`, raw OMML was absent, and the
  isolated accent mismatch rolled back before its combining-character normalization was
  fixed.
- `scripts/build_native_plugin.ps1` — self-contained native package built with no
  Python runtime.
- Two current local package builds are byte-identical: 195 files, 85,150,302 expanded
  bytes and a 36,200,235-byte ZIP at SHA-256
  `e1ef21c763cae801f48bcfa43d1513ff279936a8fd026e00b28a04133755afe8`.
  The packaged executable reports 85 operations/15 exposed tools, discovers the
  transform action and leaves the Word-process count at zero. Mandatory CI run
  `29970012359` passed all five jobs, and its downloaded Windows ZIP matched both local
  archives byte for byte at the same size and SHA-256.
- The current development head exposes 15 public tools and 85 lazy actions. Inspection,
  semantic query and transform now have transport-neutral typed Engine contracts. Query
  uses one canonical result across SDK, JSON CLI, direct MCP and indexed MCP, and is the
  first action whose output schema, version, permission and reversibility metadata are
  explicit in the inspected action contract; capability v1 keeps its original closed
  operation-summary shape and reports only the resulting coverage counts. Its checked-in
  five-match request produced a
  Draft-2020-12-valid 3,195-byte compact result; its 9,461-byte complete action contract
  remains below the 10,000-byte budget. Fifteen cold CLI runs measured 197.76 ms median
  and 206.68 ms p95 without starting Word. Two supported package builds produced
  byte-identical 36,214,740-byte ZIPs and identical 195-file trees at SHA-256
  `04af23655f262389c16a9b8eb94cc08d39173c5a9d80e74606929d26f9beb9e9`;
  mandatory CI run `29973644971` passed all five jobs and its downloaded Windows ZIP
  matched both local archives byte for byte. The packaged executable returned the same
  schema-valid query result without Word. Transform also feeds the public benchmark
  protocol.
- The preceding capability checkpoint exposed 15 public tools and 84 lazy actions. Its
  read-only capability gateway and CLI share one vendor-neutral manifest generated from
  the embedded action schema. It retains the schema/MCP/compatibility header, reports
  deterministic source and native-action hashes, pages effect summaries, publishes hard
  limits and exposes missing permissions/reversibility/output-version coverage as zero
  instead of inventing it. It does not open Word or read a document.
  Local proof covers 340 engine tests, 239 native tests and the complete 1,273-test
  Python/OOXML suite. Two self-contained builds produced the same 195-file,
  36,179,845-byte ZIP at SHA-256
  `1ebf765215f58ca22de6204a382dcedd83ec9e51dae15a9ba1c47509b237627f`.
  The packaged CLI/MCP payloads matched, the retrievable Draft 2020-12 schema validated,
  and discovery changed neither the zero Word-process count nor the document state.
- The preceding table checkpoint exposed 14 public tools and 84 lazy actions. Two local
  builds produced the same 195-file, 85,056,480-byte tree and 36,172,619-byte ZIP with
  SHA-256
  `910a1cece37397f61568ea0a230fe663fdf54d834d8a8d367fa56b37ddfe1c13`.
  Through the packaged executable, the advanced torture DOCX produced 2 tables, 34 rows
  and 251 cells with zero table issues in a 2,308-character complete response. It opened
  no Word process, returned no source part path, cell text or raw XML and used no Python.
  Mandatory CI run `29959678412` passed all five jobs with zero annotations. Its downloaded Windows artifact
  matched both local ZIPs and the 195-file expanded tree byte for byte.
- The preceding chart-graph checkpoint exposed 14 public tools and 81 lazy actions. Two
  local builds and mandatory CI run `29947553481` produced the same 195-file,
  84,535,811-byte tree and 36,034,548-byte ZIP with SHA-256
  `26b7c1ff41933a26a2cd812aec70abe2f74408005eba8a76d48918409f2f0f88`.
- The preceding semantic-golden checkpoint exposed 14 public tools and 80 lazy actions.
  Its two local builds and mandatory CI run `29944487747` produced the same 195-file,
  84,409,303-byte expanded tree and 35,998,031-byte ZIP at SHA-256
  `bdbeaf9414e1b56311c6518c49787a76dc700da0fff184cbf62f8bb2c0079ed5`.
- The preceding unused-style-deletion checkpoint exposed 14 public tools and 80 lazy
  actions. Two local
  builds produced the same 195-file, 84,397,476-byte tree and 35,995,361-byte ZIP with
  SHA-256
  `417af08694ba33cd7c6fedc6ad06a6e2baf00b51f0f2af76c34d703b3976f9b6`.
  Through the packaged executable, a clone first created one unused custom style in the
  Apache POI style fixture. A 55-character `delete_unused_style` command produced a
  934-character compact plan with one negative byte delta, no new SDK errors and the same
  plan ID under reversed JSON property order. Apply matched its predicted fingerprint,
  changed only `word/styles.xml`, retained a byte-exact pre-apply backup, removed the
  selected style and preserved every original ZIP-entry payload. It returned no XML and
  did not alter the running Word process set. A packaged attempt to delete the default
  `Standard` style failed closed without changing the file. All five jobs in mandatory
  CI run `29938834101` passed, and its hosted Windows ZIP matched the local archive byte
  for byte at the same size and SHA-256.
- The preceding exact-consolidation checkpoint matched mandatory CI run `29936319636`
  exactly at SHA-256
  `c7f68a994a185b80e211a751a05881537f0d836c53e195107cd4ae2fd4d11a76`; all five jobs
  passed. Its 108-character command produced a 902-character compact plan, rewrote six
  references and removed one exact custom duplicate with a retained backup.
- The preceding bulk-selector checkpoint matched mandatory CI run `29928388511`
  exactly at SHA-256
  `65ccaed09b608be6fb851a4ae38e54b4fc4ff9d1b7ac8b6f1e5aaa9d0dcbaeac`; all five jobs
  passed. Its 200-target regression fixture reduced serialized command input from 19,014
  to 186 characters (99.02%).
- The preceding exact semantic-style checkpoint matched mandatory CI run `29925881340`
  exactly at SHA-256
  `fc92ec8ae48273f972426934497b1217bd0d660fd55f6fa7f001b36215eb06e4`; all five jobs
  passed. Its packaged proof changed one exact source-linked paragraph, validated the
  candidate, retained a recovery backup and changed only `word/document.xml`.
- The preceding structural-query checkpoint matched mandatory CI run `29923044649`
  exactly at SHA-256
  `f416bfbd8e37adcce2a3a88a97ad4f7e5b698001d9d9f2c4784b9e15715899fe`; all five jobs
  passed. Its packaged MCP indexed a real 194-node equation DOCX, used
  `descendant_relation` to scan 11 candidates, returned 5 equation-bearing paragraphs,
  explicitly released the handle, left the source unchanged and did not alter the Word
  process set.
- The preceding semantic-index checkpoint matched hosted Windows CI run `29920866791`
  exactly at SHA-256
  `5c4b3ef4d420259463d2cca0e7ebef8d647781c541be761025861c1a73db004a`.
  It indexed the 142-node LibreOffice TOC fixture with 160 property occurrences and
  narrowed a paragraph query to 13 candidate nodes.
- The preceding 0.35 repair checkpoint exposed 14 public tools and 77 lazy actions. Two
  local builds and hosted Windows CI produced the same 195-file, 84,193,862-byte tree and
  35,935,327-byte ZIP with SHA-256
  `49aae752c5d2457d4474f63ff1142fe5909bac20cfdf7b534e97044dd3e29ca8` and no Python
  files. The packaged MCP exposed the embedded lint/repair schemas, found the exact
  source-linked empty-title finding in `lo_toc_preserve.docx`, reported one implemented
  fix, planned a candidate with both engine and baseline-aware Open XML validation
  passing, atomically created a new file, changed only `docProps/core.xml`, matched the
  predicted fingerprint, removed the target finding, returned no raw title/XML and kept
  Word unopened and the source unmodified.
- The preceding linter-only checkpoint was identical across the existing checkout, a
  clean detached worktree and hosted Windows CI: 195 files, 35,918,887-byte ZIP,
  SHA-256 `e0d162feac71679efedfeac0de6982447f4856298d9b7334a0195a04c27f7400`.
- Packaged 0.34.0 exposed 14 public tools, discovered the new dependency action and
  inspected the field-heavy LibreOffice TOC fixture as 205 nodes and 255 edges without
  opening Word or following an external target. Its compact result data was 2611
  serialized characters; the complete JSON-RPC line was 6750 characters because the
  compatibility layer mirrors the payload in both `content` and `structuredContent`.
  Two independent 195-file builds produced identical 35,867,498-byte ZIPs with SHA-256
  `f4625c2c15827e78c9b5c54eaa50adf6aeeb64644235cafc46aa8374812b3944`;
  the native runtime assembly SHA-256 is
  `d93d12ab573a72547bc4db1992c997c1cb44c6b235439ca72ec1abd16d45840f`.
- Packaged 0.33.0 compact equation preflight returned 339 serialized characters and
  omitted converted linear math. The same packaged executable exposed 14 token-lean
  tools, resolved the 73-action lazy catalog, exercised all 48 live actions through the
  public gateways through 122 MCP requests, inserted 12 native equations including
  four MathML styles and separate OMML run/control styles, reopened the saved DOCX,
  validated it
  with the Open XML SDK, exported PDF and closed its own test document. The separate
  forced equation gates matched canonical readback across the 48-family atlas, all 112
  registered symbols, all 20 named functions, ten delimiter forms, mathematical
  alphabets and an ordinary derivative. Two Windows PowerShell 5.1 builds produced
  identical 36,989,699-byte ZIPs with SHA-256
  `8a64ed4f9b69b80f338de5c30bc687852bf29d24bddcb9b99eb078d15e06d1b1`.
  A separate packaged live test left the Word cursor at offset zero, appended sentinel
  text at document end, then proved that single-equation `target="cursor"` returned an
  equation range beginning at zero under a fresh selection token.
- Packaged 0.29.0 `plan_ooxml_merge` and `apply_ooxml_merge` were executed directly
  through the released native MCP executable against the 16-entry showcase package.
  The stateless plan produced deterministic `wtmerge_`/`wtmergeapply_` identities,
  zero conflicts and a materialized no-op candidate; apply created a new DOCX, refused
  overwrite semantics, returned the exact predicted package fingerprint and did not
  start Word or Python.
- Unpacked 0.27.0 MCP lazy search, schema inspection and semantic comparison were
  exercised against real Word and Pandoc tracked-change/move fixtures. The compact
  summary was 1207 UTF-8 bytes and reported 18 package-entry plus 49 semantic
  differences; a redacted three-change page was 2150 bytes. Matching uncertainty stayed
  explicit, no sensitive values or raw XML were returned, no mutation occurred, Word was
  not started and the runtime spawned zero Python children.
- Packaged/native MCP graph inspection was exercised end to end.
- Saved-package review inspection was exercised end to end against WordToolkit,
  Mammoth, Pandoc and Apache POI comments, tracked revisions and move fixtures; default
  structured responses remained approximately 1.2–1.7 KB and raw XML/personal text was
  absent.
- Saved-package accept/reject planning and exact inverse were exercised against real
  Word, Pandoc and Apache POI tracked-revision/move fixtures. Native plan/apply tests prove
  redacted author selection, explicit-all enforcement, no Word startup, atomic backup and
  baseline-aware Microsoft Open XML SDK validation.
- Semantic inspection was exercised through lazy search -> schema inspection -> execute.
- Cross-domain dependency inspection was exercised without Word against constructed
  resolved, missing, external and orphan targets and the field-heavy LibreOffice TOC
  fixture. Stable graph identities repeated exactly, every edge endpoint existed,
  one-to-four-hop traversal stayed bounded, sensitive keys/source stayed redacted by
  default and the default response remained below 5000 serialized characters.
- Header-story query -> guarded plan -> atomic apply was exercised end to end while
  proving that the main document part remained byte-identical.
- Section inheritance and effective header/footer selection were exercised against
  constructed edge cases and a bundled Apache POI fixture.
- Style-part discovery, defaults, latent metadata, inheritance failures and token-bounded
  MCP inspection were exercised against constructed edge cases and every bundled DOCX
  style part.
- Effective paragraph/run property ordering, direct overrides, standard toggles,
  source provenance, explicit coverage omissions and a real header story were exercised
  through the engine; filtered MCP resolution was exercised without invoking Word COM.
- Numbering discovery, abstract/instance/override resolution, start overrides,
  numbering-style indirection/cycles, malformed references, effective-formatting
  precedence and token-bounded MCP inspection were exercised against constructed edge
  cases and every bundled DOCX numbering part. The POI, Mammoth and Pandoc list fixtures
  produced zero numbering-graph diagnostics.
- Reference inspection paired bookmark ranges and parsed nested complex/simple fields
  across every bundled DOCX fixture, kept main/header/text-box/note stories isolated,
  resolved case-insensitive REF dependencies, classified inert external fields, and
  proved default redaction plus a 1090-character packaged summary without invoking
  Word COM or following an external target.
- OfficeMath inspection covered every standard OMML object family, Strict markup,
  display/inline and cross-story placement, malformed/unknown/extension cases, stable
  source-derived IDs and bounded failure paths. The tracked corpus contains 23 native
  equations across three repository documents; all three pass Microsoft Open XML SDK
  validation, and the default MCP response stays below 5000 serialized characters
  without formula text, raw OMML, Word startup or conversion.
- Theme relationship/content/root validation, all color/font/format inventories,
  deterministic font and tint/shade resolution, direct composite overrides,
  provenance, quantization diagnostics and token-bounded MCP inspection were exercised
  against constructed edge cases and every theme part in the bundled DOCX corpus.

These numbers prove only the rows they touch. They do not collapse the remaining work.
